<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Character</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-9 text-white ">
        <div class="nav mb-5 text-blue-400">
            <a href="/read">Wikipedia -></a>
        </div>
        <h1 class="text-3xl tracking-tighter text-zinc-500 mb-3 ">Edit Character</h1>
        <form action="/edit/<%= user._id %>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-700 mt-3 rounded-lg bg-transparent text-white" type="text" name="name" value="<%= user.name %>" placeholder="Name" required>
            <input class="px-5 py-2 border-2 border-zinc-700 rounded-lg bg-transparent ml-2 text-white" type="text" name="passion" value="<%= user.passion %>" placeholder="Passion" required>
            <input class="px-5 py-2 border-2 border-zinc-700 rounded-lg bg-transparent ml-2 text-white" type="text" name="image" value="<%= user.image %>" placeholder="Image url" required>
            <input class="px-5 py-2 bg-blue-500 border-2 border-zinc-700 rounded-lg ml-20 text-white" type="submit" value="Update">
        </form>
    </div>
</body>
</html>
