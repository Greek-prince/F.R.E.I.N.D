<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-9 text-white ">
        <a  class = "text-blue-400 "href="/">Back To Home Page</a>
        
            <h1 style="font-size:2rem; color:var(--primary); font-weight:600; margin-bottom:1em;">All Characters</h1>
    <div style="display:flex; flex-wrap:wrap; gap:2em;">
        <% if (users.length > 0) { %>
            <% users.forEach(function(user) { %>
                <div class="card" style="width:260px;">
                    <div style="width:100%; height:180px; background:var(--secondary); border-radius:var(--radius); overflow:hidden; display:flex; align-items:center; justify-content:center;">
                        <img src="<%= user.image %>" alt="" style="max-width:100%; max-height:100%; object-fit:cover; border-radius:var(--radius);">
                    </div>
                    <h3 style="margin-top:1em; font-size:1.2rem; color:var(--foreground); font-weight:600;"><%= user.name %></h3>
                    <div style="color:var(--accent); font-size:1em; margin-bottom:0.5em;">Passion: <%= user.passion %></div>
                    <div style="display:flex; justify-content:space-between;">
                        <a href="/edit/<%= user._id %>" class="btn" style="background:var(--accent);">Edit</a>
                        <a href="/delete/<%= user._id %>" class="btn" style="background:#e11d48;">Delete</a>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div style="color:#888;">No characters found.</div>
        <% } %>
    </div>
           

        
       
    </div>

</body>
</html>