<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-9 text-white ">
        <a  class = "text-blue-400 "href="/"><-𝘉𝘢𝘤𝘬 𝘛𝘰 𝘏𝘰𝘮𝘦 𝘗𝘢𝘨𝘦</a>
        
            <h1 class="text-3xl tracking-tighter mt-4 text-zinc-200 mb-4">• 𝘍. 𝘙. 𝘐. 𝘌. 𝘕. 𝘋. 𝘚</h1>
            <div class="users flex-gap-3flex-wrap">
                <% if (users.length>0){%>
                    <% users.forEach(function(user){%>
                        <div class="user w-72 p-8 bg-zinc-800 rounded-lg">
                            <div class="w-full h-52 rounded-lg bg-zinc-600 overflow-hidden">
                                <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                            </div>
                            <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                            <h5 class="text-blue-400"><%= user.passion%></h5>
                            <div class="flex-gap-3 mt-5">
                                <a class="text-yellow-200" href="/edit/<%= user._id %>">Key Moments</a>
                                <a class = "text-red-500 ml-10"href="/delete/<%= user._id %>"> Delete User</a>
                            </div>
                        </div>
    
                    <%})%>

                <%}else{%>
                    <h3 class ="text-zinc-500">We are updating..Kindly Wait</h3>

                <%}%>
                
                
                
                

            </div>

        
       
    </div>

</body>
</html>